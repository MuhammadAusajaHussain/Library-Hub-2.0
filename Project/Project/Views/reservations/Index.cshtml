@model IEnumerable<Project.Models.reservation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.reservation_timestamp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.expiration_timestamp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.reservation_status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.book.title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user.user_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.reservation_timestamp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.expiration_timestamp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.reservation_status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.book.title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user.user_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.reservation_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.reservation_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.reservation_id })
        </td>
    </tr>
}

</table>
